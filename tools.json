[
    {
        "name": "web_search",
        "description": "Searches the web for current information on a topic.",
        "input_schema": {
            "type": "object",
            "properties": {
                "query": {
                    "type": "string",
                    "description": "The search query"
                }
            },
            "required": ["query"]
        }
    },
    {
        "name": "whois_lookup",
        "description": "Performs a WHOIS lookup for a domain. Returns registrar, registration/expiry dates, nameservers, and registrant info when available. Use this for passive reconnaissance once a target domain is known.",
        "input_schema": {
            "type": "object",
            "properties": {
                "domain": {
                    "type": "string",
                    "description": "The domain to look up, e.g. 'example.com' (no scheme, no path)"
                }
            },
            "required": ["domain"]
        }
    },
    {
        "name": "dns_lookup",
        "description": "Performs DNS enumeration on a domain. Queries A, AAAA, MX, NS, TXT, SOA, and CNAME records by default. Use this for active DNS enumeration after identifying a target domain.",
        "input_schema": {
            "type": "object",
            "properties": {
                "domain": {
                    "type": "string",
                    "description": "The domain to enumerate, e.g. 'example.com' (no scheme, no path)"
                },
                "record_types": {
                    "type": "array",
                    "items": {"type": "string"},
                    "description": "Optional list of record types to query. Defaults to ['A','AAAA','MX','NS','TXT','SOA','CNAME']. Valid types include any DNS RR type supported by dnspython (e.g. CAA, SRV, PTR, DNSKEY)."
                }
            },
            "required": ["domain"]
        }
    },
    {
        "name": "subdomain_enum",
        "description": "Enumerates subdomains for a target domain by querying crt.sh (certificate transparency logs). Returns a deduplicated, sorted list. Passive reconnaissance — no traffic to the target.",
        "input_schema": {
            "type": "object",
            "properties": {
                "domain": {
                    "type": "string",
                    "description": "Apex domain to enumerate, e.g. 'example.com'"
                },
                "limit": {
                    "type": "integer",
                    "description": "Optional max number of subdomains to return (default 100)"
                }
            },
            "required": ["domain"]
        }
    },
    {
        "name": "http_fingerprint",
        "description": "Performs HTTP fingerprinting on a URL or domain. Returns status, server, technology hints (X-Powered-By, Set-Cookie patterns), security headers, redirect chain, and the page title. Useful for tech-stack identification.",
        "input_schema": {
            "type": "object",
            "properties": {
                "target": {
                    "type": "string",
                    "description": "URL or bare domain (https:// is added automatically if missing)"
                }
            },
            "required": ["target"]
        }
    },
    {
        "name": "port_scan",
        "description": "Async TCP port scan with optional banner grab. ACTIVE reconnaissance — sends real packets to the target. Defaults to the nmap top-100 TCP ports. Only use against hosts you have explicit permission to test.",
        "input_schema": {
            "type": "object",
            "properties": {
                "host": {
                    "type": "string",
                    "description": "Hostname or IP to scan. Hostnames are resolved via system DNS."
                },
                "ports": {
                    "type": "array",
                    "items": {"type": "integer"},
                    "description": "Optional explicit port list. If omitted, scans the nmap top-100 TCP ports."
                },
                "timeout": {
                    "type": "number",
                    "description": "Per-port connect timeout in seconds (default 1.5)"
                },
                "concurrency": {
                    "type": "integer",
                    "description": "Max concurrent connections (default 200)"
                },
                "banner": {
                    "type": "boolean",
                    "description": "Attempt a tiny banner read on open ports (default true)"
                }
            },
            "required": ["host"]
        }
    },
    {
        "name": "ssl_inspect",
        "description": "Inspects the TLS certificate of a host:port. Returns subject, issuer, SANs, validity window, days remaining, signature algorithm, key size, and negotiated TLS version/cipher. Passive — single TLS handshake, no scanning.",
        "input_schema": {
            "type": "object",
            "properties": {
                "host": {
                    "type": "string",
                    "description": "Hostname to connect to (SNI). Bare domain, no scheme."
                },
                "port": {
                    "type": "integer",
                    "description": "TLS port (default 443)"
                }
            },
            "required": ["host"]
        }
    },
    {
        "name": "directory_bruteforce",
        "description": "ACTIVE web path enumeration. Sends HTTP GETs for a wordlist of common paths (admin, .git, .env, robots.txt, api, swagger, ...) and reports interesting status codes. Builtin wordlist ~60 paths. Only run against hosts you have permission to test.",
        "input_schema": {
            "type": "object",
            "properties": {
                "url": {
                    "type": "string",
                    "description": "Base URL or bare domain. https:// is prepended if missing."
                },
                "paths": {
                    "type": "array",
                    "items": {"type": "string"},
                    "description": "Optional custom wordlist. Defaults to a builtin ~60-entry list."
                },
                "concurrency": {
                    "type": "integer",
                    "description": "Max concurrent requests (default 20)"
                },
                "timeout": {
                    "type": "number",
                    "description": "Per-request timeout in seconds (default 10)"
                }
            },
            "required": ["url"]
        }
    },
    {
        "name": "http_methods",
        "description": "ACTIVE check of allowed HTTP methods on a URL. Reads the OPTIONS Allow header AND actively probes GET/HEAD/PUT/DELETE/PATCH/TRACE. WARNING: PUT/DELETE/PATCH against a misconfigured server could mutate state. Only run with explicit permission.",
        "input_schema": {
            "type": "object",
            "properties": {
                "url": {
                    "type": "string",
                    "description": "Target URL. https:// is prepended if missing."
                }
            },
            "required": ["url"]
        }
    },
    {
        "name": "vhost_discovery",
        "description": "ACTIVE virtual host enumeration. Sends requests with fuzzed Host headers (e.g. admin.target.com, staging.target.com) and reports responses that differ from a bogus-host baseline. Finds vhosts that aren't in public DNS.",
        "input_schema": {
            "type": "object",
            "properties": {
                "target": {
                    "type": "string",
                    "description": "IP or hostname to connect to. https:// is prepended if missing."
                },
                "base_domain": {
                    "type": "string",
                    "description": "Domain to prepend the wordlist to (e.g. 'example.com' -> 'admin.example.com')"
                },
                "hostnames": {
                    "type": "array",
                    "items": {"type": "string"},
                    "description": "Optional custom prefix wordlist. Defaults to a builtin ~60-entry list."
                },
                "concurrency": {
                    "type": "integer",
                    "description": "Max concurrent requests (default 15)"
                },
                "timeout": {
                    "type": "number",
                    "description": "Per-request timeout in seconds (default 8)"
                }
            },
            "required": ["target", "base_domain"]
        }
    },
    {
        "name": "write_file",
        "description": "Writes content to a file in the output directory.",
        "input_schema": {
            "type": "object",
            "properties": {
                "file_path": {
                    "type": "string",
                    "description": "File path relative to the output directory"
                },
                "content": {
                    "type": "string",
                    "description": "Content to write"
                }
            },
            "required": ["file_path", "content"]
        }
    }
]
